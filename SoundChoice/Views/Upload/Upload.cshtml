@model SoundChoice.Models.ViewModels.UploadViewModel
@inject Microsoft.AspNetCore.Identity.SignInManager<SoundChoice.Models.ApplicationUser> signInManager

@if (signInManager.IsSignedIn(User))
{
    <h2>Upload</h2>
    <form id="upload" method="post" enctype="multipart/form-data" asp-action="Upload" asp-controller="Upload">
    <h4>Enter your file's properties so it can be found easily.</h4>
    <hr />
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class=form-group row>
            <label asp-for="Title" class="col-md-2"></label>
            <div class="col-md-10">
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
    </div>
    <!--
    <div class=form-group row>
        <label asp-for="Type" class=col-md-2></label>
        <div class="col-md-10">
                <select asp-for="Type" asp-items="" class="form-control"></select>
                <span asp-validation-for="Type" class="text-danger"></span>
            </div>
    </div>
    <div class=form-group row>
        <label asp-for="Genre" class=col-md-2></label>
        <div class="col-md-10">
                <select asp-for="Genre" asp-items="" class="form-control"></select>
                <span asp-validation-for="Genre" class="text-danger"></span>
            </div>
    </div>
    <div class=form-group row>
        <label asp-for="BPM" class=col-md-2></label>
        <div class="col-md-10">
                <input asp-for="BPM" class="form-control" />
                <span asp-validation-for="BPM" class="text-danger"></span>
            </div>
    </div>
    -->
    <div class=form-group row>
        <label asp-for="File" class=col-md-2></label>
        <div class="col-md-10">
                <input type="file" name="File" />
                <span asp-validation-for="File" class="text-danger"></span>
        </div>
    </div>
    <div class=form-group row>
        <button type="submit">Submit</button>
    </div>
    </form>
    @section Scripts{
         <partial name=_ValidationScriptsPartial />
    }
}
else
{
    <header>Sign in in order to upload.</header>
}
