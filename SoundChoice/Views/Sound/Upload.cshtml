@model SoundChoice.Views.Sound.UploadPhysicalModel
@inject Microsoft.AspNetCore.Identity.SignInManager<SoundChoice.Models.ApplicationUser> signInManager

@if (signInManager.IsSignedIn(User))
{
   /* <h2>Upload</h2>
<form id="sound" method="post" asp-action="Upload" asp-controller="Sound">
    <h4>Enter your file's properties so it can be found easily.</h4>
    <hr />
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class=form-group row>
            <label asp-for="Title" class="col-md-2"></label>
            <div class="col-md-10">
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
    </div>
    <div class=form-group row>
        <label asp-for="Type" class=col-md-2></label>
    </div>
   
    </form>
     
     <h2>Upload</h2>
    <form id="sound" method="post" asp-action="Upload" asp-controller="Sound">
    <h4>Enter your file's properties so it can be found easily.</h4>
    <hr />
   
    </form>
    <form enctype="multipart/form-data" method="post">
    <dl>
        <dt>
            <label asp-for="FileUpload.FormFile"></label>
        </dt>
        <dd>
            <input asp-for="FileUpload.FormFile" type="file">
        </dd>
        <dd><input asp-page-handler="Upload" class="btn-bg-success" type="submit" value="Upload"></dd>
        <a asp-action="Details" asp-controller="Sound" id="detailsLink"><dd class="btn bg-success text-white" >Next</dd></a>
    </dl>
    
   
    </form>
    */
     <h2>Upload</h2>
    
<form enctype="multipart/form-data" method="post">
    <dl>
        <dt>
            <label asp-for="FileUpload.FormFile"></label>
        </dt>
        <dd>
            <input asp-for="FileUpload.FormFile" type="file"/>
            <span asp-validation-for="FileUpload.FormFile"></span>
        </dd>
    </dl>    
    <input asp-page-handler="Upload" class="btn" type="submit" value="Upload" />
</form>

}
else
{
    <header>Sign in to upload.</header>
}
